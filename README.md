# quake-pipeline

This is my implementation of the Kafka service that talks to the external API, i.e., USGS.
To run this code, please install the required programs locally and run the script `up.sh` from the root directory.